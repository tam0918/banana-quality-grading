# --- Python / Virtualenv ---
.venv/
venv/
ENV/
__pycache__/
*.py[cod]
*.pyd
*.so
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# --- OS / Editor ---
.DS_Store
Thumbs.db
.vscode/

# --- Training artifacts / datasets (often huge) ---
# Roboflow/Kaggle datasets
/datasets/
/datasets_smoke/

# Ultralytics/YOLO training runs
/runs/

# Model weights
# Default: ignore all heavy model artifacts everywhere...
# *.pt  # Allow .pt files to be committed
*.onnx
*.engine

# ...but allow committing selected weights under /weights/ (careful!).
# Recommended: only commit small demo weights (e.g., best.pt). For large weights, use Git LFS.
/weights/**
!/weights/
!/weights/*.pt

# --- Fonts (often large and may have licensing constraints) ---
/assets/fonts/*.ttf
/assets/fonts/*.otf

# --- Logs ---
*.log
